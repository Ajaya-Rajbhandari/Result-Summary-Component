// Load the JSON Data
fetch("data.json")
  .then((response) => response.json())
  .then((data) => {
    const scoreElement = document.getElementById("score");
    const maxScore = document.getElementById("");
  });
